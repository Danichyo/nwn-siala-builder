# Скопировать в .env на сервере и заполнить. Файл .env в git НЕ попадает.
#
# SECRET_KEY_BASE генерируется командой:  mix phx.gen.secret
# POSTGRES_PASSWORD — любая длинная случайная строка, например:
#   openssl rand -base64 32

SECRET_KEY_BASE=
POSTGRES_PASSWORD=
